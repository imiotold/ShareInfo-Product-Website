{
    "compilerOptions": {
        "baseUrl": ".",
        "paths": {
            "@/*": ["./*"]
        },
        "jsx": "react-jsx", // or "react" for older versions of React
        "target": "ES6", // or higher version if needed
        "module": "ESNext"
    }
}
